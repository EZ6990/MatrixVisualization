#:include View/matrixcell.kv

<MatrixView>:
    spacing: 5
    #on_press: self.zoom(args[1],args[2])
    on_touch_down: self.zoom(args[1])

    canvas.before:
        Color:
            rgba: (0/255,0/255,0/255, 1)
        Rectangle:
            pos: self.pos
            size: self.size